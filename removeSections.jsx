//-----------------------------------------------------// Remove existing sections, part of:// Automated side-by-side page numbering for InDesign// // Author: Andrew Heiss (www.andrewheiss.com)// Date: 2013-08-06//-----------------------------------------------------// sideBySideNumbering.jsx only works on pages that have no sections applied.// If a page has a section, the script will fail.  This becomes an issue if you need to change page numbering after running the side-by-side script,// since you technically can't run the script again without removing all the script-generated sections. // This simple script removes sections from a range of pages.//---------------// Script setup//---------------// Determine the absolute number of the page by selecting the page in InDesign and running the following in the ExtendScript Toolkit JavaScript Console:// app.activeWindow.activePage.documentOffset;var startingPage = 1  // Absolute page number, zero-based. var endingPage = 4  // Absolute page number, zero-based. //----------------// Actual script//----------------var myDoc = app.activeDocument;  // Create document objectfor (var i=startingPage; i<=endingPage; i++) {    var myPage = myDoc.pages[i];  // Load the current page    var mySection = myPage.appliedSection;        try {mySection.remove()} catch (e) {};  // Remove the applied section (if any) for the page}